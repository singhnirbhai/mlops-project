apiVersion: apps/v1
kind: Deployment
metadata:
  name: earthquake-api
  labels:
    app: earthquake-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: earthquake-api
  template:
    metadata:
      labels:
        app: earthquake-api
    spec:
      containers:
      - name: earthquake-api
        image: your-dockerhub-username/diabetes-api:latest
        ports:
        - containerPort: 8000
        imagePullPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: earthquake-api-service
spec:
  selector:
    app: earthquake-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
